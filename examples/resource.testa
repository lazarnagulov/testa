// Directives 
@output csv; 
@seed 42;

// Customize data resources
resource Name {
    first = ["John", "Peter"];
    last = ["Doe", "Smith"];
}

// Generate
generate _ [10] {
    id = $uuid();
    first_name = $pick(Name.first);
    last_name = $pick(Name.last);
}