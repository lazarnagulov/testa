[package]
name = "testa-hir"
version = "1.0.0"
edition = "2024"

[features]
test-utils = []

[dependencies]
testa-core = { path = "../testa-core" }
serde = { version = "1", features = ["derive"] }
rmp-serde = "1" 